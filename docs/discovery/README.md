# Discovery Artifacts

This directory holds discovery artifacts produced by real `/discover` runs.

When you invoke `/discover` on a problem, Phase 4 writes the artifact to `docs/discovery/<topic-slug>.md` here.

For test/evaluation outputs from running `/discover` against eval prompts (not real problems), see `docs/discover-evals/` — those are evidence about the skill's behavior, not actual discovery work.
