---
name: greet
description: Generate a friendly greeting message
when_to_use: When the user asks for a greeting or wants to say hello
arguments: [name]
---

Greet the user with a friendly, personalized message.

If a name was provided via `$name`, address them by name. Otherwise, use a generic friendly greeting.

Keep it brief — one or two sentences.
